# A2T Budget Control Report

This Evidence dashboard reads daily recommend-only outputs generated by the pipeline.

```sql pacing
select * from read_csv_auto('../data/output/daily_pacing.csv')
```

## Daily Pacing

<DataTable data={pacing} />

```sql recommendations
select * from read_csv_auto('../data/output/campaign_recommendations.csv') order by priority asc
```

## Campaign Recommendations

<DataTable data={recommendations} />

```sql channels
select * from read_csv_auto('../data/output/channel_summary.csv') order by spend_30d desc
```

## Channel Summary

<DataTable data={channels} />

```sql decisions
select * from read_csv_auto('../data/output/decision_log.csv')
```

## Decision Log

<DataTable data={decisions} />

```sql pacing_history
select * from read_csv_auto('../data/output/history_daily_pacing.csv') order by report_date
```

## Pacing History

<DataTable data={pacing_history} />
